spring:
  profiles:
    active: dev


logging:
  level:
      root: info
  path: /tmp/logs
  config: classpath:logback-config.xml

lzcloud:
  starter:
    syslog:
      module: lzcloud_member
      table-name: system_log